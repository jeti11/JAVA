public class Access {
  // 접근 제한자 : 클래스 및 클래스의 구성 멤버에 대한 접근을 제한하는 역할
  // public, protected, default, private
  // public : 제한 없음
  // protected : 상속받은 경우 사용가능
  // default : 같은 패키지는 사용가능, 다른 패키지에서는 import로 가져와도 사용 못함(public, private 생략한 경우)
  // private : 제한 (내부에서만 사용)
}
